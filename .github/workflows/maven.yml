name: Java CI

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  call_workflow:
    uses: asekawa/Java-codecov-sample/.github/workflows/build-template.yml@main
    secrets: inherit
